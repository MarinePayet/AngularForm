import { RangeDateDirective } from './range-date.directive';

describe('RangeDateDirective', () => {
  it('should create an instance', () => {
    const directive = new RangeDateDirective();
    expect(directive).toBeTruthy();
  });
});
